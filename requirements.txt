# Installation (à la racine du dépôt Pacing/) :
#   python -m venv .venv
#   source .venv/bin/activate
#   pip install -e .          # recommandé (pyproject.toml)
#   # ou : pip install -r requirements.txt
#   playwright install chromium
#
# Python 3.10+ requis.
#
# Lancer :
#   Desktop   : python -m pacing.ui.desktop.app
#   Web       : python -m pacing.ui.web.app   (API FastAPI doit tourner)
#   DearPyGUI : python -m pacing.ui.dearpygui.app  (API FastAPI doit tourner)
#   API       : uvicorn pacing.api.main:app --reload
#   Notebooks : jupyter notebook notebooks/

# --- API FastAPI ---
fastapi>=0.100.0
uvicorn[standard]>=0.22.0
pydantic>=2.0.0

# --- HTTP & scraping (Extranat, Omega, USA Swimming) ---
requests>=2.31.0
beautifulsoup4>=4.12.0

# --- Extraction PDF Omega via Claude ---
anthropic>=0.40.0
python-dotenv>=1.0.0

# --- Navigateur headless (Omega, token USA Swimming) ---
playwright>=1.40.0

# --- Données & cache Parquet ---
pandas>=2.0.0
pyarrow>=14.0.0
numpy>=1.24.0

# --- Visualisation ---
matplotlib>=3.7.0
seaborn>=0.13.0

# --- Interface desktop Flet ---
flet>=0.24.0
flet-desktop>=0.24.0

# --- Interface web NiceGUI (client API) ---
nicegui>=2.0.0
httpx>=0.27.0

# --- Prototype desktop DearPyGUI (client API) ---
dearpygui>=1.11.0

# --- Notebooks Jupyter ---
jupyter>=1.0.0
notebook>=7.0.0
ipykernel>=6.25.0

# --- Tests ---
pytest>=8.0.0
