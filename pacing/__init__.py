"""Package principal Pacing : analyse du pacing en natation.

Couches :
- ``config`` — chemins et settings
- ``domain`` — labels, normalisation, schémas
- ``ingestion`` — scraping et ETL par source
- ``data`` — loaders / repositories
- ``analytics`` — calculs purs (sans matplotlib)
- ``rendering`` — tracé matplotlib
- ``application`` — use cases et orchestration graphes
- ``api`` / ``ui`` — présentation
"""

__version__ = "0.2.0"
