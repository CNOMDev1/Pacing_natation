"""Modèles et schémas de données partagés par l'application Pacing.

Ce package centralise les types Python utilisés par les routers FastAPI,
l'interface desktop et les pipelines de données :

- ``extranat_models`` — schémas Pydantic du JSON brut Extranat scrapé
- ``frmn_models`` — schémas Pydantic des JSON FRM Natation (Maroc)
- ``usaswimming_models`` — schémas Pydantic des enregistrements USA Swimming
"""
